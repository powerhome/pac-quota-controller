apiVersion: quota.powerapp.cloud/v1alpha1
kind: ClusterResourceQuota
metadata:
  name: test-clusterresourcequota-quota
spec:
  namespaceSelector:
    matchLabels:
      quota: limited
  hard:
    pods: "5"
    limits.cpu: "2"
    limits.memory: 2Gi
    requests.cpu: "1"
    requests.memory: 1Gi
